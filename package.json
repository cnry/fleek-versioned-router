{
  "name": "fleek-versioned-router",
  "version": "0.0.2",
  "description": "Extends fleek-router to use multiple swagger spec versions",
  "main": "src/router.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "fleek-parser": "git@github.com:fleekjs/fleek-parser.git",
    "fleek-router": "^1.0.1",
    "fleek-validator": "git@github.com:cnry/fleek-validator.git#fix-path-params",
    "koa": "^1.1.2",
    "koa-compose": "^2.3.0",
    "koa-mount": "^1.3.0",
    "koa-static": "^2.0.0",
    "lodash": "^4.2.1",
    "path": "^0.12.7",
    "swagger-model-validator": "^2.0.7"
  },
  "devDependencies": {
    "glob": "^6.0.4"
  }
}
